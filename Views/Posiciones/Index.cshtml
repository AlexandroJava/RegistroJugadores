@model IEnumerable<RegistroJugadores.Models.Posicion>

@{
    ViewBag.Title = "Index";
}

<h2>Posiciones</h2>

@{
    if (User.IsInRole("Administrador"))
    {
<p>
    @Html.ActionLink("Create New", "Create")
</p>
    }
    }
<div class="table-responsive">
    <table class="table-bordered table-condensed table-hover table-striped table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomPosicion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstadoPosicion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaPosicion)
            </th>
@{
    if (User.IsInRole("Administrador"))
    {
            <th>Opciones</th>
    }
    }
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.NomPosicion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EstadoPosicion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FechaPosicion)
                </td>
@{
    if (User.IsInRole("Administrador"))
    {
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.PosicionID }) |
                    @Html.ActionLink("Details", "Details", new { id = item.PosicionID }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.PosicionID })
                </td>
        }
    }
            </tr>
        }

    </table>
</div>

<p>
    @Html.ActionLink("Regresar a la Pagina Principal", "Index", "Home")
</p>
